'use strict';

var _asyncToGenerator2 = require('babel-runtime/helpers/asyncToGenerator');

var _asyncToGenerator3 = _interopRequireDefault(_asyncToGenerator2);

var _promise = require('babel-runtime/core-js/promise');

var _promise2 = _interopRequireDefault(_promise);

var _index = require('./index');

var _themerColorsDefault = require('themer-colors-default');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('render', () => {

  const promisedFiles = _promise2.default.all((0, _index.render)(_themerColorsDefault.colors));

  it('should render the expected number of files', (0, _asyncToGenerator3.default)(function* () {
    const files = yield promisedFiles;
    expect(files.length).toBe(2);
  }));

  it('should render valid Slack theme strings', (0, _asyncToGenerator3.default)(function* () {
    const files = yield promisedFiles;
    files.forEach(function (file) {
      const contents = file.contents.toString('utf8');
      expect(/\,\,/.test(contents)).toBe(false);
      expect(/\s/.test(contents)).toBe(false);
    });
  }));
});