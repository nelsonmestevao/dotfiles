# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: cmps
    replace: |-
      Cumprimentos,
      {{name}}.
    vars:
      - name: name
        type: shell
        params:
          cmd: git config --global user.name
  - trigger: ":sig"
    replace: |-
      Sincerely,
      {{name}}.
    vars:
      - name: name
        type: shell
        params:
          cmd: git config --global user.name
  - trigger: "@@"
    replace: "{{email}}"
    vars:
      - name: email
        type: shell
        params:
          cmd: git config --global user.email
  - trigger: ":@"
    replace: "{{username}}"
    vars:
      - name: username
        type: shell
        params:
          cmd: git config --global github.user
  - trigger: ":nemo"
    replace: "{{name}}"
    vars:
      - name: name
        type: shell
        params:
          cmd: git config --global user.name
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%d/%m/%Y"
  - trigger: ":ip"
    replace: "{{myip}}"
    vars:
      - name: myip
        type: shell
        params:
          cmd: "ifconfig wlp3s0 | awk '$1 == \"inet\" {print $2}'"
  - trigger: ":hb"
    replace: |
      Feliz aniversário, $|$! 🎂