# /home/nelson/.dotfiles/tmux/projects/spells.yml

name: spells
root: ~/.spells

startup_window: editor

windows:
  - shell: exa --long --group-directories-first --git
  - editor:
      layout: main-vertical
      panes:
        - vim
        - git status --verbose

